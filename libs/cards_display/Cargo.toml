[package]
name = "cards_display"
version = "0.1.0"
edition = "2024"

[dependencies]
cards = { path = "../cards" }

[features]
default = ["unicode"]
unicode = []
ascii = []
jokers = ["cards/jokers"]

[dev-dependencies]
proptest = "1.7.0"
